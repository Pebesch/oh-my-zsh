# oh-my-zsh!

Those are my (opinionated) two cents on how my shell should be configured.

## Setup

Copy the repo somewhere on your machine and create a symlink from `~/.zshrc` to `<repo-location>/.zshrc`.

E.g. `ln -s ~/repos/oh-my-zsh/.zshrc ~/.zshrc`

